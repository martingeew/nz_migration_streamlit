.firecrawl/
__pycache__/
.playwright-mcp/
itm-snapshot.md
blog/

# Screenshots
*.png
.venv/

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Quarto build cache
dashboard/.quarto/

# Pre-generated chart HTML (regenerated by build_dashboard.py before each render)
dashboard/data/

# docs/ is committed to repo (GitHub Pages serves from it — do NOT ignore)

# ── Scrollytelling prototypes ────────────────────────────────────────────────
# Source is committed; build output, deps, and generated data are not.
prototypes/*/node_modules/
prototypes/*/dist/
prototypes/data/
prototypes/01-closeread/charts/
prototypes/01-closeread/index.html
prototypes/01-closeread/index_files/
prototypes/01-closeread/.quarto/
prototypes/02-observable/src/.observablehq/

# ── Scrollytelling story (scrolly/) ──────────────────────────────────────────
# src/data/story.json IS committed so `npm run dev` works without running Python.
scrolly/node_modules/
scrolly/dist/
